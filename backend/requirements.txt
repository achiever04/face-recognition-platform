# backend/requirements.txt
# Pinned versions for Python 3.10 environment (adjust if you want looser ranges)

# Web / API
fastapi==0.116.1
uvicorn==0.35.0
# Starlette must be in the range fastapi expects: >=0.40.0,<0.48.0
starlette==0.45.0

# Validation / env
pydantic==2.11.7
python-dotenv==1.1.0

# Database
pymongo==4.15.0

# Upload parsing
python-multipart==0.0.20

# Scientific / Imaging / Video
numpy==2.2.6
# Use headless variant for server environments (no X display required)
opencv-python-headless==4.12.0.88
Pillow==11.3.0
imageio==2.37.0
moviepy==1.0.3

# Face recognition stack
face_recognition==1.3.0
face_recognition_models==0.3.0
dlib==20.0.0

# Deep learning (PyTorch)
# NOTE: For GPU systems you should install the appropriate torch wheel for your CUDA.
torch==2.8.0
torchvision==0.23.0

# InsightFace & ONNX runtime
insightface==0.7.3
onnxruntime==1.22.1
onnx==1.19.0

# Index/search
faiss-cpu==1.7.4

# Networking / async / sockets
requests==2.32.4
aiohttp==3.9.4
# Use a compatible redis client
redis==5.3.1
python-socketio==5.11.2

# Notifications
twilio==8.11.1

# Utilities / ML helpers
scikit-learn==1.7.2
tqdm==4.67.1

# Optional / recommended utilities
cryptography==45.0.5
python-dateutil==2.9.0.post0

# Notes:
# - If you run the backend in a headless environment, opencv-python-headless is recommended.
# - For GPU-enabled PyTorch, install the torch + torchvision wheel matching your CUDA.

